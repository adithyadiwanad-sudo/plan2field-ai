import {test,expect} from '../../frontend/node_modules/@playwright/test/index.mjs';
import {login} from './helpers.js';
test.skip(process.env.RUN_REAL_E2E!=='1','Real database/model stack not enabled');
test('offline draft survives reload and uploads on reconnect',async({page,context})=>{test.skip(!process.env.RUN_REAL_E2E,'Requires production shell cache and real database');await login(page);await page.getByRole('link',{name:'Submit report',exact:true}).click();await page.evaluate(()=>navigator.serviceWorker.ready);await context.setOffline(true);await page.getByLabel('Field report',{exact:true}).fill('Line 24 erection started on 12 September 2026.');await page.getByRole('button',{name:'Submit report',exact:true}).click();await expect(page.getByText('1 saved on this device')).toBeVisible();await page.reload();await expect(page.getByText('1 saved on this device')).toBeVisible();await context.setOffline(false);await page.getByRole('button',{name:'Sync now'}).click();await expect(page.getByText('0 saved on this device')).toBeVisible();});
