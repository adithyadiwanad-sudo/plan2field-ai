# Python 3.11 recommended.
# imageio-ffmpeg supplies a portable FFmpeg binary.
#
# Install:
# python -m pip install -r requirements.txt
#
# Start:
# python -m streamlit run app.py --theme.base=dark

streamlit>=1.50,<2
pandas>=2.2,<3
plotly>=6.0,<7
pydantic>=2.10,<3
numpy>=1.26,<3
openai-whisper==20250625
imageio-ffmpeg>=0.6,<0.7

# Optional cloud fallback, disabled by default in the UI.
openai>=2.0,<3

# SQLite, pathlib, difflib and other standard-library modules
# do not need separate pip packages.
#
# Native st.audio_input provides browser recording.
# No st_audiorec dependency is required.